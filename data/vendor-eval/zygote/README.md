# Zygote evaluation drops (proprietary — gitignored)

Backup vendor path for future evaluation (PR #20: SciePro preferred; Zygote optional).

Drop samples under `source/` only. Never copy into `public/`.
See `docs/SCIEPRO_EVALUATION_HARNESS.md`.
